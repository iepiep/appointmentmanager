{% extends '@PrestaShop/Admin/layout.html.twig' %}

{% block content %}
    <div class="card">
        <div class="card-block">
            <h4 class="card-title">{{ 'Customer List Page'|trans({}, 'Modules.Appointmentmanager.Admin') }}</h4>
            <p class="alert alert-info">{{ text_customer_list }}</p>
        </div>
    </div>
{% endblock %}