{% extends '@PrestaShop/Admin/layout.html.twig' %}

{% block content %}
    <div class="card">
        <div class="card-block">
            <h4 class="card-title">{{ 'Configuration Page'|trans({}, 'Modules.Appointmentmanager.Admin') }}</h4>
            <p class="alert alert-info">{{ text_config }}</p>
        </div>
    </div>
{% endblock %}